package com.cryptoportfoliotracker.logic;
/*
import com.cryptoportfoliotracker.entities.Asset;
import com.cryptoportfoliotracker.repository.TransactionRepository;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import java.util.ArrayList;

@Configuration
public class TransactionConfig {
    @Bean
    public TransactionManager TransactionConfig() {

        TransactionRepository list = (TransactionRepository) new ArrayList<Asset>();

        return new TransactionManager(list);
    }
}*/